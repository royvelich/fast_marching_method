% load("maze.mat");
% imshow(I);
% F = double(I);

A = [1,2,3,4,5];
B = [4,5,6,7,8];

C = cat(A, B);

bla = vecnorm(C, 2, 3)

D = repmat(vecnorm(C, 2, 3), [1 1 2]);

% load('pool.mat');

% dist = dist_mex(F, [815, 384], [0], 'silent');
% bla = dist .* F;
% 
% N = normalize(dist)
% imagesc(bla)